@page "/"
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="flex-grow flex items-center justify-center">
    <div class="container mx-auto p-4 max-w-md">
        <h1 class="text-3xl font-bold mb-4 text-center">File Upload</h1>
        <div id="dropZone" class="border-2 border-dashed border-primary rounded-lg p-8 text-center cursor-pointer mb-4">
            <span class="drop-zone__prompt">Drop file here or click to upload</span>
            <input type="file" name="myFile" id="fileInput" class="hidden">
        </div>
        <div id="fileInfo" class="mb-4 text-center"></div>
        <button id="processBtn" class="btn btn-primary w-full mb-4" disabled>
            <i class="bi bi-upload mr-2"></i>
            Process
        </button>
        <div class="w-full bg-gray-200 rounded-full h-2.5 dark:bg-gray-700">
            <div id="progressBar" class="bg-primary h-2.5 rounded-full" style="width: 0%"></div>
        </div>
        <div id="progressText" class="text-center mt-2">0%</div>
        <toast-button/>
    </div>
</div>